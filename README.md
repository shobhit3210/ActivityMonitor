# Team Activity Monitor

Ask natural questions about what team members are working on – pulls live data from JIRA + GitHub.

## Setup

1. `cp .env.example .env` and fill in your credentials
2. `npm install`
3. `npm start`
4. Open http://localhost:3000
